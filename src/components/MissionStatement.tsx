import styles from './MissionStatement.module.css';

export default function MissionStatement() {
  return (
    <section className={styles.section}>
      <p className={styles.text}>
        I craft narratives across mediums. Whether framing a shot through a lens or mapping a user’s journey through an interface, the goal is to design experiences that resonate.
      </p>
    </section>
  );
}
